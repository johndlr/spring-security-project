spring:
  application:
    name: spring-security-project
  datasource:
    url: jdbc:mysql://localhost:3306/spring_security_jwt
    username: admin
    password: admin
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
