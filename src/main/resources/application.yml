server:
  port: 8080

spring:
  application:
    name: spring-security-project
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true 
    hibernate:
      ddl-auto: update
