server:
  port: 8080

spring:
  application:
    name: spring-security-project
  datasource:
    url: jdbc:mysql://api-rest-jwt-server.mysql.database.azure.com:3306/users_db?useSSL=true
    username: juandlr
    password: azure@12345
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
